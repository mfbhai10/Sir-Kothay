# Static HTML client reads API base from static/js/api-config.js (see SIR_KOTHAY_API_BASE).
# These names are reserved if you later add a Vite/Next build that injects env at compile time.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
VITE_API_URL=http://127.0.0.1:8000
